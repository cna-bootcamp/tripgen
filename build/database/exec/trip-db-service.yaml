apiVersion: v1
kind: Service
metadata:
  name: trip-db-service
  namespace: tripgen-dev
  labels:
    app: trip-db
    component: database
    service: trip-service
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: trip-db